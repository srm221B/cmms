{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "chmod +x railway-build.sh && ./railway-build.sh && cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
} 